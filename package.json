{
  "name": "drone-mission-planner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local-first WPML mission planner that keeps every waypoint inside a legal boundary",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@turf/turf": "^7.2.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.1.9",
    "vitest": "^3.2.4"
  }
}
